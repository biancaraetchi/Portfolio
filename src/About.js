import './About.css';

const About = ({...props}) => {
    return (
        <div className="mainContent">
            <h2>About the project : to be continued</h2>
        </div>
    );
}

export default About;